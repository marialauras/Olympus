const db_username = "postgres";
const db_password = "rafael12";
const host = "localhost";
const port = 5432;
const db_name = "tp2";

module.exports = {
    db_username,
    db_password,
    host,
    port,
    db_name
}